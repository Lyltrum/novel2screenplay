# 待办 / Backlog

后续可做的增强（按价值排序），均为可选，不影响当前已交付主干。

## 1. 评估层 + 消融实验（最高价值）
用 LLM-as-judge 对剧本多维打分（忠实度 / 冲突强度 / 视觉化程度 / 对白自然度），
并量化对比"有/无 story bible""有/无修复闭环""有/无 craft 约束"的差异。
> 价值：用数据证明各亮点真的提升质量，竞赛/论文含金量最高。

## 2. 把 craft 用起来：剧本结构体检报告
基于每场的 `craft.function` 统计全剧戏剧结构：
- SETUP 与 PAYOFF 是否配对（埋了伏笔有没有回收）
- ESCALATE_CONFLICT 的分布是否体现张力递增
- 是否存在连续多场同质职能（节奏问题）
> 产出一份"结构体检"附在转换结果里，进一步把"好剧本"可视化。

## 3. 校验阈值与词表调优
- 信息倾倒阈值（当前单句 > 80 字）是否合适，按真实语料校准
- "展示而非讲述"的心理描写词表（当前 13 个）是否需要扩充/收敛，降低误报
> 价值：减少修复闭环的无效抖动，提升精度。

---

## 已完成（参考）
- 核心管线、来源可追溯、自检修复闭环、Fountain 导出、风格可选
- 长文规模化（分块 + 滚动提要 + 去重）、交互式精修
- 编剧笔记 craft 注释层 + 展示而非讲述/对白去信息倾倒硬约束
- 可选 demo 前端（见 `web/`，非课题要求）

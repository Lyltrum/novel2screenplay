spring:
  application:
    name: novel2screenplay
  ai:
    dashscope:
      # API key 走环境变量，绝不入库；未配置时留空，应用仍可启动，仅在真正调用模型时报错
      api-key: ${AI_DASHSCOPE_API_KEY:}
      chat:
        options:
          # 开发期 qwen-plus；终稿可切 qwen-max，超长章节可切 qwen-long（改环境变量即可）
          model: ${SCREENPLAY_MODEL:qwen-plus}
          # 抽取任务要稳定、少发挥，温度调低
          temperature: 0.3

server:
  port: 8080
